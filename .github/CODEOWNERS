# Branch protection on main requires a code-owner review; this makes that rule
# real for outside contributions (the repo owner can still merge own PRs since
# enforce_admins is off).
* @HarmenSchouten
