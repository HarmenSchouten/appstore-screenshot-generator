The instructions for coding agents in this repository live in `CLAUDE.md` at
the repository root, with area rules in `.claude/rules/`. Follow them, and see
`docs/ARCHITECTURE.md` for the reasons behind them.
