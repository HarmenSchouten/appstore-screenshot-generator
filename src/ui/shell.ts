/**
 * UI Shell Module
 * 
 * Contains the main UI HTML generator function.
 */

import { GLOW_COLORS } from '../renderer.ts';
import type { ProjectConfig, ProjectInfo } from '../types/index.ts';

// Constants needed for UI
export { GLOW_COLORS };

// Import from lib
import { GRADIENT_TEMPLATES, DEFAULT_PALETTES } from '../lib/index.ts';
export { GRADIENT_TEMPLATES, DEFAULT_PALETTES };

// Re-export types
export type { ProjectConfig, ProjectInfo };
